<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/client/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <title>Push noti</title>
</head>
<body>
    <img id="bg" src="/client/nature_bg.jpg"/>
    <form  id="send-push__form">
        <div id="left" class="p-3">
            <h3 class="header text-center" >Send a push notification</h3>
            <p class="error"></p>
            <%if (status == "stopping"){%>
            <button  class="btn btn-light" class="text-center" id="sendingBtn">Send Me</button>
            <%}else{%>
            <button  class="btn btn-light" class="text-center" id="sendingBtn" disabled>Sending the push ... Click stop to stop</button>
            <%}%>
        </div>
        <div>
            <div class="form-group">
                <label for="time">Total time</label>
                <div class="line">
                    <input class="form-control" type="number" value="<%=data.time%>" id="example-number-input">
                    <select class="form-control form-control-md ml-2">
                        <option
                        <%if(data.time_unit == "Second"){  %>
                            selected
                        <%}%>
                        >Second</option>
                        <option
                        <%if(data.time_unit == "Hours"){  %>
                            selected
                        <%}%>
                        >Hours</option>
                        <option
                        <%if(data.time_unit == "Minute"){  %>
                            selected
                        <%}%>
                        >Minute</option>
                    </select>
                </div>
            </div>
            <div class="form-group">
                <label for="time">Reapeat time</label>
                <div class="line">
                    <input class="form-control" type="number" value="<%=data.interval%>" id="example-number-input">
                    <select class="form-control form-control-md ml-2">
                        <option
                        <%if(data.interval_unit == "Second"){  %>
                            selected
                        <%}%>
                        >Second</option>
                        <option
                        <%if(data.interval_unit == "Hours"){  %>
                            selected
                        <%}%>
                        >Hours</option>
                        <option
                        <%if(data.interval_unit == "Minute"){  %>
                            selected
                        <%}%>
                        >Minute</option>
                    </select>
                </div>
            </div>
            <div class="form-group">
                <label for="exampleFormControlTextarea1">Message</label>
                <textarea class="form-control" id="exampleFormControlTextarea1" rows="3" placeholder="Message right herer"><%=data.message%></textarea>
            </div>
        </div>
       
    </form>
    <% if (status== "stopping") { %>
    <button id="stop_push" class="btn btn-light stop" class="text-center"  disabled >Stop Disabled</button>
    <% } else {%>
    <button id="stop_push" class="btn btn-light stop" class="text-center"  >Stop</button>
    <%}%>
    
    <script src="/client/client.js"></script>
</body>
</html>